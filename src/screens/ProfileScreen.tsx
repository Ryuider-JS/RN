import { Text, View } from 'react-native';

const ProfileScreen = () => {
  return (
    <View className="flex items-center justify-center flex-1 bg-red-500">
      <Text className="text-3xl font-bold">1</Text>
    </View>
  );
};
export default ProfileScreen;
